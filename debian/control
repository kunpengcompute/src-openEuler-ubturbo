Source: ubturbo
Section: admin
Priority: optional
Maintainer: openEuler Maintainers <maintainers@openeuler.org>
Standards-Version: 4.6.0
Build-Depends: debhelper-compat (= 13),
               make (>= 4.3),
               gcc (>= 10.3.1),
               cmake (>= 3.22),
               libvirt-dev,
               rapidjson-dev,
               ninja-build,
               patchelf,
               chrpath,
               pkg-config,
               libboundscheck-dev,
               flex,
               bison,
               libelf-dev,
               libssl-dev,
               libncurses-dev,
               linux-headers-generic [arm64] | linux-headers [arm64],
               kmod,
               dkms,
               dh-sequence-dkms
Rules-Requires-Root: binary-targets
Homepage: https://gitee.com/openeuler/ubturbo

Package: ubturbo-dkms
Architecture: arm64
Depends: ${misc:Depends}, dkms, ub-os-component-dkms-dev
Description: SMAP, ucache and ub_dma kernel modules for ubturbo (DKMS)
 This package ships the source of the SMAP driver kernel modules
 (smap_tracking_core, smap_access_tracking, smap_histogram_tracking,
 smap_tiering), the ucache kernel module and the Huawei UB-DMA kernel
 module (ub_dma). The modules are built on the target machine via DKMS
 on install and on every kernel upgrade. The ubcore/urma kernel
 headers required by ub_dma are provided by ub-os-component-dkms-dev.

Package: ubturbo-rmrs
Architecture: arm64
Depends: ${misc:Depends}, ${shlibs:Depends},
         ubturbo-smap (= ${binary:Version})
Recommends: ubturbo-ucache (= ${binary:Version})
Description: UB Turbo resource management framework
 This package contains the ubturbo daemon (ub_turbo_exec),
 client library (libubturbo_client.so), RMRS plugin,
 configuration files, and the systemd service unit.
 .
 It is the core of the ubturbo framework and depends on the smap
 and ucache companion packages shipped from the same source.

Package: ubturbo-smap
Architecture: arm64
Depends: ${misc:Depends}, ${shlibs:Depends}, kmod, ubturbo-dkms (= ${binary:Version})
Description: SMAP userspace library and udev rule for ubturbo
 This package contains the SMAP userspace library (libsmap.so) and the
 udev rule that governs device permissions. The kernel modules
 themselves are provided by ubturbo-dkms and built on the target via
 DKMS.

Package: ubturbo-ucache
Architecture: arm64
Depends: ${misc:Depends}, ${shlibs:Depends}, ubturbo-rmrs (= ${binary:Version})
Description: UCache userspace plugin for ubturbo
 This package contains the Huawei UCache userspace plugin
 (libucache_os_turbo_plugin.so) and its configuration file
 loaded by the ubturbo daemon.

Package: ubturbo-dev
Section: libdevel
Architecture: arm64
Multi-Arch: same
Depends: ${misc:Depends}, ubturbo-rmrs (= ${binary:Version})
Description: Development headers for the UB Turbo framework
 This package contains the C headers required to integrate with
 the UB Turbo SDK, including turbo_conf.h, turbo_def.h,
 turbo_ipc_client.h, turbo_ipc_server.h, turbo_logger.h,
 turbo_serialize.h, turbo_rmrs_interface.h,
 turbo_ucache_interface.h and smap_interface.h.
